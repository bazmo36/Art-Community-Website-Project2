<nav class="navbar">
    <div class="nav-left">
        <a href="/home" class="logo">🎨 Art Platform</a>
    </div>

    <!-- Search Bar -->
    <div class="nav-center">
        <form action="/artworks" method="GET" class="search-form">
            <input type="text" name="q" placeholder="Search artworks or artists..." class="search-input">
            <button type="submit" class="search-btn">🔍</button>
        </form>
    </div>

    <!-- Navigation Links -->
    <div class="nav-right">
        <% if (user) { %>
            <a href="/home">Home</a>
            <a href="/artworks">Explore</a>
            <a href="/users/profile">Profile</a>
            <a href="/auth/logout" class="logout">Logout</a>
            <% } else { %>
                <a href="/auth/login">Login</a>
                <a href="/auth/sign-up" class="btn-primary">Sign Up</a>
                <% } %>

                    <!-- Dark Mode Toggle -->
                    <button id="theme-toggle" class="theme-toggle">🌙</button>
    </div>
</nav>