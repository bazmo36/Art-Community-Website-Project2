<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Explore Artworks | Art Platform</title>
    <link rel="stylesheet" href="/css/style.css">
     <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Playwrite+AU+QLD:wght@100..400&display=swap" rel="stylesheet">
    
</head>
<body class="light">
    <%- include('../partials/navbar') %>

    <main class="container explore-page">
        <h1>Explore Artworks</h1>
        <a href="/users/profile" class="btn-secondary">⬅ Back to Profile</a>
        <hr>

        <% if (allArtworks.length > 0) { %>
            <div class="artworks-grid">
                <% allArtworks.forEach(artwork => { %>
                    <div class="artwork-card">
                        <a href="/artworks/<%= artwork._id %>">
                            <img src="<%= artwork.image %>" alt="<%= artwork.title %>" class="artwork-img">
                        </a>
                        <div class="artwork-info">
                            <p class="artwork-title"><%= artwork.title %></p>
                            <p class="artwork-artist">
                                By <a href="/users/<%= artwork.artist._id %>"><%= artwork.artist.username %></a>
                            </p>
                        </div>
                    </div>
                <% }) %>
            </div>
        <% } else { %>
            <p>No artworks uploaded yet. Be the first to share your creativity!</p>
        <% } %>
    </main>

    <%- include('../partials/footer') %>
</body>
</html>
