<h1>All Artworks</h1>
<% allArtworks.forEach(art => { %>
    <div>
        <h3><%= art.title %></h3>
        <p>By: <%= art.artist.username %></p>
        <img src="<%= art.imageUrl %>" width="200">
        <a href="/artworks/<%= art._id %>">View Details</a>
    </div>
<% }) %>
