<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= foundArtwork.title %> | Art Platform</title>
    <link rel="stylesheet" href="/css/style.css">
     <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Playwrite+AU+QLD:wght@100..400&display=swap" rel="stylesheet">
</head>
<body class="light">
    <%- include('../partials/navbar') %>

    <main class="container artwork-details-page">
        <div class="artwork-details">
            <div >
                <img src="<%= foundArtwork.image %>" alt="<%= foundArtwork.title %>"class="artwork-image">
            </div>
            <div class="artwork-meta">
                <h1><%= foundArtwork.title %></h1>
                <p><%= foundArtwork.description %></p>
                <p>Artist: 
                    <a class="artwork-artist" href="/users/<%= foundArtwork.artist._id %>">
                        <%= foundArtwork.artist.username %>
                    </a>
                </p>

                <!-- Like Button -->
                <form class="like" action="/artworks/<%= foundArtwork._id %>/like" method="POST">
                    <button type="submit" class="btn-like">
                        <% if (foundArtwork.likes.includes(user._id)) { %>
                            ❤️ Unlike
                        <% } else { %>
                            🤍 Like
                        <% } %>
                    </button>
                </form>

                <p><%= foundArtwork.likes.length %> Likes</p>

                <hr>

                <!-- Comments Section -->
                <h3>Comments</h3>
                <div class="comments-section">
                    <% if (foundArtwork.comments.length > 0) { %>
                        <% foundArtwork.comments.forEach(comment => { %>
                            <p><strong><%= comment.user.username %>:</strong> <%= comment.content %></p>
                        <% }) %>
                    <% } else { %>
                        <p>No comments yet. Be the first to comment!</p>
                    <% } %>
                </div>

                <!-- Add Comment -->
                <form action="/artworks/<%= foundArtwork._id %>/comment" method="POST" class="form">
                    <textarea name="content" placeholder="Add a comment..." required></textarea>
                    <button type="submit" class="btn-primary">Add Comment</button>
                </form>
              
                <a href="javascript:history.back()" class="btn-secondary">⬅ Go Back</a>

            </div>
        </div>
    </main>

    <%- include('../partials/footer') %>
</body>
</html>
