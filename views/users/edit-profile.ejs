<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Profile</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body class="light">
    <%- include('../partials/navbar') %>

    <main class="container">
        <h1>Edit Profile</h1>
        <% if (error) { %> 
            <p class="error"><%= error %></p>
        <% } %>

        <form action="/users/edit-profile?_method=PUT" method="POST" enctype="multipart/form-data" class="form">
            <!-- Bio -->
            <div class="form-group">
                <label for="bio">Bio:</label>
                <textarea id="bio" name="bio" rows="4" cols="50" placeholder="Write something about yourself..."><%= user.bio %></textarea>
            </div>

            <!-- Profile Picture -->
            <div class="form-group">
                <label for="profilePic">Profile Picture:</label>
                <input type="file" id="profilePic" name="profilePic" accept="image/*">
            </div>

            <button type="submit" class="btn-primary">Update Profile</button>
        </form>

        <a href="/users/profile" class="btn-secondary">⬅ Back to Profile</a>
    </main>

    <%- include('../partials/footer') %>
</body>
</html>
