<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home | Art Platform</title>
    <link rel="stylesheet" href="/css/style.css">
</head>

<body class="light">
    
    <%- include('./partials/navbar') %>

        <main class="container home-page">
            <h1>Welcome to Art Platform</h1>
            <% if (user) { %>
                <p>Hello, <strong>
                        <%= user.username %>
                    </strong>!</p>
                <div class="home-actions">
                    <a href="/users/profile" class="btn-primary">My Profile</a>
                    <a href="/artworks" class="btn-secondary">Explore Artworks</a>
                    <a href="/auth/logout" class="btn-danger">Logout</a>
                </div>
                <% } else { %>
                    <p>Join our community of artists and share your creativity.</p>
                    <div class="home-actions">
                        <a href="/auth/login" class="btn-primary">Login</a>
                        <a href="/auth/sign-up" class="btn-secondary">Sign Up</a>
                    </div>
                    <% } %>
        </main>

        <%- include('./partials/footer') %>
</body>

</html>